This project should be a part of UberDeployer.Core but UberDeployer.WinApp uses it. We'll be able to get rid of this dependency
when we move the feature of creating db versions model to the agent service.
